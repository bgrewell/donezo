/** Moved: the quick-capture dialog now lives in components/capture/ (an
 *  orchestrator plus per-kind field components). Import path kept stable. */
export { QuickCapture } from "@/components/capture/QuickCapture";
